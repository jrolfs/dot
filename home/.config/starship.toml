add_newline=false

format = """
$username\
$hostname\
$shlvl\
$kubernetes\
$directory\
$git_branch\
$hg_branch\
$git_commit\
$git_status\
$git_state\
$docker_context\
$package\
$cmake\
$dart\
$dotnet\
$elixir\
$elm\
$erlang\
$golang\
$helm\
$julia\
$nim\
$nodejs\
$java\
$ocaml\
$perl\
$php\
$purescript\
$python\
$ruby\
$rust\
$swift\
$terraform\
$zig\
$nix_shell\
$conda\
$memory_usage\
$aws\
$gcloud\
$openstack\
$env_var\
$crystal\
$cmd_duration\
$custom\
$lua\
$line_break\
$jobs\
$battery\
$time\
$status\
${custom.pair}\
$character\
"""


#
# ---------- General ----------------------------------------

[character]
success_symbol = "[→](green)"
error_symbol = "[×](red)"
vicmd_symbol = "[↔](green)"

[directory]
truncation_length = 3
truncation_symbol = "…/"
truncate_to_repo = true


#
# ---------- Context ---------------------------------------

[custom.pair]
command = "echo '--'"
when = "$XDG_CONFIG_HOME/starship/is-pairing.sh"
format = "[$symbol($output)]($style)"
style = "yellow"

[git_branch]
style = "purple"
symbol = " "
truncation_length = 30
format = "on [$symbol$branch]($style) "

[git_state]
format = '[$state( $progress_current/$progress_total)]($style) '
cherry_pick = "[🍒 picking](bold red)"
rebase = "[↑ rebasing](bold yellow)"
merge = "[→ merging](bold cyan)"
bisect = "[↕ bisecting](bold yellow)"
style = "yellow"

[git_status]
format = '([[$conflicted$stashed$deleted$renamed$modified$staged$untracked$ahead_behind](bold red)]($style) )'
style = "red"
ahead = "⇡"
behind = "⇣"
conflicted = "≠"
deleted = "×"
diverged = "↕"
modified = "◗"
renamed = "»"
staged = "+"
stashed = "⤓"
untracked = "?"
# up_to_date = "✓"

[package]
disabled = true

[gcloud]
disabled = true

[nix_shell]
impure_msg = "[⚠](bold red)"
pure_msg = "[λ](bold green)"
format = 'via [❄️ ($name·$state )](bold blue) '


#
# ---------- Languages -------------------------------------

[nodejs]
style = "green"
symbol = " "

[ruby]
symbol = " "


#
# ---------- System ----------------------------------------

[battery]
full_symbol = "🔋"
charging_symbol = "⚡️"
discharging_symbol = "💀"

[[battery.display]]
threshold = 15

[memory_usage]
disabled = true
threshold = 75
symbol = " "
style = "dimmed green"

